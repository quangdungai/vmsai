# Sao chép thành .env và điền. KHÔNG commit .env.
# Cloud GPU (Phase 1b)
VLM_ENDPOINT=
CLOUD_GPU_PROVIDER=

# Postgres / pgvector (tuỳ chọn cho M4 store)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=vms247
POSTGRES_USER=vms
POSTGRES_PASSWORD=changeme
